[package]
name = "goose-lite"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Minimal Goose embedding surface for downstream integrations"

[lints]
workspace = true

[features]
default = ["mcp-builtins"]
mcp-builtins = ["dep:goose-mcp-lite"]

[dependencies]
goose = { path = "../goose", default-features = false, features = ["lite"] }
goose-mcp-lite = { path = "../goose-mcp-lite", optional = true }

[dev-dependencies]
tokio = { workspace = true }
